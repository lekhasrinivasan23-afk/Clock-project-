# Clock-project-
A simple Digital Clock and Countdown Timer built using HTML, CSS, and JavaScript. It displays the current time in real-time (HH:MM:SS) and includes a countdown timer to a specified date using the Date() object and setInterval() function.
